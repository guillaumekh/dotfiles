# For switching between local virtual machines without ssh complaining
# about hostkeys not matching recorded keys in know_hosts
NoHostAuthenticationForLocalhost yes

# For forcing Sierra's ssh-agent to use the Keychain
Host *
  UseKeychain yes
  AddKeysToAgent yes
  IdentityFile ~/.ssh/id_rsa
